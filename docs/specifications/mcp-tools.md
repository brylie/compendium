# MCP tool surface

**Depends on:** [`data-model.md`](./data-model.md) (WorkspaceRecord shape), [`collaboration.md`](./collaboration.md) (holds)

---

All tools operate on `WorkspaceRecord` uniformly — no separate "block tools" vs. "row tools," per the PRD's unified-model requirement.

| Tool               | Input                                                                                                     | Output                                                                                                                                                 | Notes                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| ------------------ | --------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------ | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `list_documents`   | —                                                                                                         | `{id, title}[]`                                                                                                                                        |                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| `get_document`     | `documentId`                                                                                              | `{id, title, parentDocumentId?, records: {id, blockType, markdown, referencedRecordId?, linkBroken?, viewConfig?, calloutStyle?, childPagesDepth?}[]}` | `content` transcoded to Markdown at this boundary (see [`markdown-transcoding.md`](./markdown-transcoding.md)); a `page_link`, `collection_view`, or `child_pages` block's target (Document, Collection, or Document respectively) is itself permission-checked — `referencedRecordId` is included, preserved for recovery even once broken, and omitted only when the target is out of the caller's scope (never for a merely-deleted one); a `collection_view` block's `viewConfig` is included/omitted under the same rule (see [`collection-views.md`](./collection-views.md) §5); each of the three sets `linkBroken: true` once its target is deleted, rendering `[[Deleted page]]`, `[collection view: Deleted collection]`, or `[child pages: unavailable]` respectively rather than being indistinguishable from an unconfigured block (see [`internal-links.md`](./internal-links.md)) — `child_pages` uses one generic "unavailable" placeholder for both "deleted" and "out of scope", the same anti-oracle principle `create_record`'s target validation already applies; a `child_pages` block with no explicit `referencedRecordId` defaults to `documentId` itself (always resolvable) and renders its resolved children as nested `- [[Title]]` Markdown bullets, or `_No sub-pages yet._` when empty (issue #43) — `childPagesDepth` is exposed read-only the same way `calloutStyle` already is (no MCP write path beyond `create_record`'s initial value)                                                                                                                                                                                                                                                                                                                                                       |
| `create_document`  | `title, parentDocumentId?, afterDocumentId?`                                                              | `{id, title, parentDocumentId?}`                                                                                                                       | permission-checked against `parentDocumentId` when nested; persists the calling token's own access grant to the new Document as part of the same call (see [`service-layer.md`](./service-layer.md))                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| `move_document`    | `documentId, parentDocumentId?, afterDocumentId?`                                                         | `{success: boolean}`                                                                                                                                   | permission-checked against both `documentId` and the new `parentDocumentId`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| `delete_document`  | `documentId`                                                                                              | `{success: boolean}`                                                                                                                                   | deletes the Document and its child tree                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| `list_collections` | —                                                                                                         | `{id, title, schema, primaryFieldKey?}[]`                                                                                                              | `primaryFieldKey` is the _resolved_ primary field (see [`collection-views.md`](./collection-views.md) §7) — the field an agent should treat as a record's human-readable display title, not the raw, possibly-unset stored value; a record's canonical identity remains its `id`, never a mutable field value                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| `query_collection` | `collectionId, filter?`                                                                                   | `{id, title, schema, primaryFieldKey?, rows: {id, properties}[]}`                                                                                      | currently a direct `Y.Doc` read via the service layer, not the Drizzle-managed read model [`persistence.md`](./persistence.md) §2 describes for it (that read model isn't built yet — see [`collection-views.md`](./collection-views.md) §8); `filter` is currently accepted but unused (a pre-existing gap, not specific to any view) — an agent gets the same unfiltered rows regardless of how a human's Table/Board/Calendar view (see [`collection-views.md`](./collection-views.md)) is filtered, sorted, or summarized; this is a deliberate two-surface design for now, not an oversight — see [`collection-views.md`](./collection-views.md) §9 for how UI `ViewConfig` semantics and MCP query semantics relate and what closing this gap would mean; `primaryFieldKey` is resolved the same way as `list_collections`' above (see §7)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| `search_workspace` | `query, space_id?`                                                                                        | `{recordId, snippet}[]`                                                                                                                                | full-text over block content + text/select properties, via the same read model (SQLite FTS5); `space_id` (#133) restricts results to one Space — omitted, searches every Space in the workspace (Phase 0's single-Space default). Passed, the uncataloged-fallback scan (content predating #113's catalog) is skipped entirely, since that content has no reliably known Space membership to check — see `data-model.md` §4                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| `hold_records`     | `recordIds: string[]`                                                                                     | `{granted: string[], denied: string[]}`                                                                                                                | see [`collaboration.md`](./collaboration.md); permission-checked per record — a denied record is never granted or has its hold renewed, regardless of any other record in the same request                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| `write_record`     | `recordId, markdown \| properties \| referencedRecordId \| viewConfig \| viewConfigPatch`                 | `{success: boolean}`                                                                                                                                   | `markdown` requires an active hold for existing block content, atomic content-write + hold-release; `referencedRecordId` retargets an existing `page_link` or `collection_view` block — a metadata write, not content, so it needs no hold (same exemption as `properties`), and is idempotent by construction; the new target must be an existing, caller-accessible Document (`page_link`) or Collection (`collection_view`), or the call is rejected (see `create_record`'s note on the shared validation); `viewConfig` (issue #37) replaces an existing `collection_view` block's entire `EmbeddedViewConfig` wholesale — only accepted on that block type, `viewType` must be `table`/`board`/`calendar`, and a member absent from the new value is cleared rather than left as-is (this is `create_record`'s initial-value shape, not the UI's per-member `patchRecordViewConfig`); `viewConfigPatch` (issue #195) instead merges only the named members into an already-configured `collection_view` block's config, leaving every other member exactly as-is — the MCP-facing counterpart to the UI's `patchRecordViewConfig`, so an agent that only means to change e.g. `filters` doesn't have to round-trip and resupply every other member (which would otherwise silently clobber whatever another actor concurrently set on a member it never touched); a member is explicitly cleared with `null` (JSON has no way to send an omitted-vs-undefined distinction directly) or left untouched by omitting it; only accepted on an already-configured `collection_view` block (`viewConfig` covers the initial configure), and mutually exclusive with `viewConfig` in the same call — see [`collection-views.md`](./collection-views.md) §9                                                                            |
| `release_records`  | `recordIds: string[]`                                                                                     | `{success: boolean}`                                                                                                                                   | explicit abandon, without writing                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| `create_record`    | `parentId, afterRecordId?, blockType? \| properties?, referencedRecordId?, viewConfig?, childPagesDepth?` | `{recordId}`                                                                                                                                           | no hold needed — no prior content to protect; `blockType` is accepted only when `parentId` names a Document; Collection rows are created without it, optionally with `properties`; `referencedRecordId` sets a `page_link`, `child_pages`, or `collection_view` block's target in the same call — only accepted when `blockType` is one of those three and `parentId` names a Document; for `child_pages` and `page_link` it's optional (absent means "current Document"/"unconfigured link" respectively), `collection_view`'s target is optional too (an unconfigured embed); when supplied, the target must be an existing, caller-accessible Document (`page_link`/`child_pages`) or Collection (`collection_view`), else the call is rejected with one generic "not an accessible Document"/"not an accessible Collection" error per kind that never distinguishes "doesn't exist" from "exists but out of scope" (see [`internal-links.md`](./internal-links.md) §4, [`audit-coverage.md`](./audit-coverage.md) §3); `viewConfig` (issue #37) sets a `collection_view` block's `EmbeddedViewConfig` in the same call — only accepted when `blockType` is `collection_view`, and `viewType` must be `table`/`board`/`calendar`; `childPagesDepth` (a positive integer, or `"unlimited"`) sets a `child_pages` block's nesting depth in the same call — only accepted when `blockType` is `child_pages`, absent defaults to 1 (immediate children only); reconfiguring `childPagesDepth` on an existing `child_pages` block after creation is UI-only (`setRecordChildPagesConfig`, no `write_record` support), the same "no MCP write path beyond creation" precedent `calloutStyle` already established — `referencedRecordId`/`viewConfig` differ: both remain writable post-creation via `write_record` (see its row above) |
| `delete_record`    | `recordId`                                                                                                | `{success: boolean}`                                                                                                                                   | no hold needed                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |

**Permission scoping:** each access token (see [`persistence.md`](./persistence.md) §1) carries an allowlist of Document/Collection IDs, checked with operation-specific rules rather than one uniform record-parent check: `get_document`, `query_collection`, and document mutations (`create_document`, `move_document`, `delete_document`) authorize the target Document/Collection ID itself; record operations (`hold_records`, `write_record`, `create_record`, `delete_record`, `release_records`) authorize the target record's `parentId`; `list_documents`, `list_collections`, and `search_workspace` filter their results down to allowed Document/Collection IDs rather than authorizing a single target; and `move_document` validates both the moved `documentId` and the destination `parentDocumentId`. [`workspace-sharding.md`](./workspace-sharding.md) (#112, approved) §5 extends this same scoping into a trusted-routing rule at the MCP boundary once #113 lands, resolving a token's Workspace/Space scope and target shard before any hold or write — a denied record's Awareness/`Y.Doc` state is only ever read (to resolve it and to run the check itself), never mutated on its behalf. This is what "per-agent permission scoping" means concretely, even single-tenant: a token scoped to one Document can't read or write anything else, which is directly testable per the PRD's permission-denied acceptance criterion. A denied call is itself audited (as `<action>_denied`, carrying only the target ID the caller already supplied) — see [`audit-coverage.md`](./audit-coverage.md) §3 — so a trust review can see repeated denied attempts, not just successful ones.

**Space-level grants (#6):** a token also carries an `allowedSpaceIds` allowlist (`access_tokens.allowed_space_ids`, a JSON array mirroring the per-Document/per-Collection columns), set via `settings/tokens`' "Allowed Spaces" fieldset — there is no MCP-exposed way for a token to grant itself Space access. `tokenAllowsParent` (`src/lib/mcp/tokens.ts`) composes the two: a target is authorized if it's directly allowlisted by ID _or_ its own catalog Space is one of the token's `allowedSpaceIds` — resolved live against the record's current catalog `spaceId` (via `resolveShardForParent`/`resolveParentWorkspaceContext`) on every check, not backfilled onto individual records, so a Space grant automatically covers content created in that Space afterward. This composes with every operation-specific rule above unchanged (a `get_document` call still authorizes the target Document ID, just via either gate); `list_documents`/`list_collections` need no `space_id` param of their own for this to work — an unscoped listing call already reflects a Space-scoped token's grant through the same composed check. Not yet exposed: MCP-driven Space creation, and `create_document`/`create_collection` targeting a token's granted Space from MCP (both remain workspace-default-only for now).
