# Block capability contract

**Depends on:** [`prd.md`](../prd.md) — especially [Block editor interaction](../prd.md#block-editor-interaction-formatting--slash-command-insertion) and [Core Architectural Principle](../prd.md#core-architectural-principle-unified-data-model); [`data-model.md`](./data-model.md); [`rich-text-toolbar.md`](./rich-text-toolbar.md); [`markdown-transcoding.md`](./markdown-transcoding.md); [`collaboration.md`](./collaboration.md) (holds).

---

## 1. Purpose and scope

Every `BlockType` Compendium ships — existing or new — must define an explicit, checked capability contract before it reaches the slash-command menu, the toolbar, or the MCP surface. This exists because 0.4.0 alone adds four new block types (columns, math, diagram, and a richer bookmark/embed) on top of the existing catalogue: implementing each as a one-off would silently reintroduce per-block special cases into a model whose entire value proposition is uniform record/permission/hold/MCP handling (see Core Architectural Principle). This is not a new approval gate — it makes explicit and checkable the acceptance-criteria detail the PRD's block-editor requirements already imply.

## 2. Contract fields

A `BlockType`'s implementation issue must fill out all eleven fields before it merges:

1. **Icon + label** — slash-menu and toolbar display.
2. **Slash-command keywords/aliases** — including any conventional aliases (e.g., `/toggle`, `/details`, `/collapsible` all resolving to the same block).
3. **Content shape** — leaf text-bearing, structural, or container-with-children. Reuses the text-bearing/structural distinction `rich-text-toolbar.md` §5 already establishes; a container type (columns, toggle) additionally declares its child-block ordering semantics.
4. **Conversion compatibility** — which other types it converts to/from in place without data loss, per `rich-text-toolbar.md` §5's in-place-conversion vs. insert-new-block rule.
5. **Focus/Enter/Backspace/Escape behavior** — how the block participates in split/join editing (see the PRD's Enter/Backspace requirements) and what Escape does inside a container type.
6. **Drag/multi-select/group behavior** — whether the block can be part of a multi-select group action, and any constraints (e.g., a container's children move as a unit with their parent).
7. **Configuration UI and valid defaults** — if the block has settings (e.g., a callout's color variant, a diagram's render error state).
8. **Markdown read/write representation** — the block's shape at the MCP boundary per `markdown-transcoding.md`, including its graceful fallback when a connected client can't render it natively.
9. **Agent-readable structured representation** — only needed where Markdown is insufficient to express the block's full state (e.g., a `collection_view` block's `viewConfig`).
10. **Hold semantics** — whether this block type is safe to hold/replace/patch atomically during an agent write, per `collaboration.md`; a container type must state whether a hold on it implies holds on its children.
11. **Accessible static/read-only rendering** — screen-reader and keyboard-only access to the block's content independent of any live-editing affordance (e.g., a Mermaid diagram's raw source must remain available to assistive tech, since the rendered SVG isn't meaningfully accessible on its own).

## 3. Worked examples

### Toggle / collapsible (shipped, P0)

| Field                 | Value                                                                                                                                                                                                             |
| --------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Icon + label          | Chevron icon; "Toggle list"                                                                                                                                                                                       |
| Keywords              | `/toggle`, `/collapsible`, `/details`                                                                                                                                                                             |
| Content shape         | Container-with-children (one summary text run + an ordered list of child blocks)                                                                                                                                  |
| Conversion            | Converts to/from any text-bearing type by acting on its summary text only; converting away from toggle does not delete its children — they're re-parented to the toggle's former position                         |
| Focus/Enter/Backspace | Enter in the summary splits it like any text-bearing block; Enter at the end of the last child creates a new child; Backspace at the start of the first child does nothing special (does not collapse the toggle) |
| Drag/multi-select     | Dragging the toggle moves it and all children as a unit; children are independently selectable/movable within the toggle                                                                                          |
| Configuration         | None beyond expanded/collapsed state (a per-viewer UI state, not CRDT-synced content)                                                                                                                             |
| Markdown              | `<details><summary>...</summary>...</details>` per `markdown-transcoding.md`                                                                                                                                      |
| Agent representation  | Markdown is sufficient                                                                                                                                                                                            |
| Hold semantics        | Holding a toggle holds only its summary text by default; holding a specific child holds that child alone                                                                                                          |
| Accessible rendering  | Native `<details>`/`<summary>` semantics carry keyboard and screen-reader support for free                                                                                                                        |

### Diagram / Mermaid (#151, new in 0.4.0)

| Field                 | Value                                                                                                                                                                                                                |
| --------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Icon + label          | Diagram icon; "Diagram (Mermaid)"                                                                                                                                                                                    |
| Keywords              | `/diagram`, `/mermaid`                                                                                                                                                                                               |
| Content shape         | Leaf (a single stored text run — the Mermaid source — with no child blocks)                                                                                                                                          |
| Conversion            | Converts to/from `code` (treated as a structural type per its render behavior, not a text-bearing one — see below) by preserving the raw source text; does not convert to/from prose types like paragraph or heading |
| Focus/Enter/Backspace | Structural block behavior: Enter/Backspace act on the block as a whole (create/remove), not on inline text, matching `code`'s existing treatment                                                                     |
| Drag/multi-select     | Standard single-block drag; no children                                                                                                                                                                              |
| Configuration         | None; render errors (invalid Mermaid syntax) show inline with the raw source, not a blank block                                                                                                                      |
| Markdown              | Fenced code block with a `mermaid` language tag (` ```mermaid `), matching GitHub/GitLab's own convention                                                                                                            |
| Agent representation  | Markdown is sufficient — this is exactly why Mermaid was chosen over a freeform diagram editor: an agent can read and produce valid input deterministically                                                          |
| Hold semantics        | Safe to hold/replace/patch as a single leaf value, identical to a `code` block                                                                                                                                       |
| Accessible rendering  | The raw Mermaid source is always available to assistive tech (e.g., as the rendered SVG's accessible description or an adjacent toggle to view source) — the rendered diagram alone is not sufficient                |

### Child pages (#43, shipped)

| Field                 | Value                                                                                                                                                                                                                                                                |
| --------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Icon + label          | Page-tree icon; "Child pages"                                                                                                                                                                                                                                        |
| Keywords              | `/child pages`, `/page tree`, `/subpages`, `/children`, `/index`                                                                                                                                                                                                     |
| Content shape         | Leaf (no stored `content`; a computed listing, like `table_of_contents` — see `data-model.md` §3)                                                                                                                                                                    |
| Conversion            | No in-place conversion to/from any other type — a computed block, same as `table_of_contents`                                                                                                                                                                        |
| Focus/Enter/Backspace | Structural block behavior: Enter/Backspace act on the block as a whole, matching `table_of_contents`/`collection_view`'s existing treatment                                                                                                                          |
| Drag/multi-select     | Standard single-block drag; no children of its own (its rendered list is read-only navigation, not editable child blocks)                                                                                                                                            |
| Configuration         | Target Document (`referencedRecordId`, absent = the current Document) and nesting depth (`childPagesDepth`, absent = 1); set via a small settings menu on the block itself, not the slash menu — see `rich-text-toolbar.md` §8                                       |
| Markdown              | Nested `- [[Title]]` bullets, one per resolved child, indented per nesting level; `_No sub-pages yet._` when empty; `[child pages: unavailable]` when an explicit target doesn't resolve — see `markdown-transcoding.md`                                             |
| Agent representation  | Markdown is sufficient for the resolved listing itself; `referencedRecordId`/`childPagesDepth` are exposed read-only on `get_document` (`mcp-tools.md`) since they're configuration, not renderable content — the same role `viewConfig` plays for `collection_view` |
| Hold semantics        | Safe to hold/replace/patch as a single leaf value; in practice never holds content, since there's nothing to write (creation only accepts `referencedRecordId`/`childPagesDepth`, never `markdown`)                                                                  |
| Accessible rendering  | A native `<ul>`/`<li>` list of real `<a href>` links — full keyboard/screen-reader navigation with no separate accessible-mode fallback needed                                                                                                                       |

### Columns (#148, new in 0.4.0)

| Field                 | Value                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| --------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Icon + label          | Two-panel columns icon; "Columns"                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| Keywords              | `/columns`, `/column`, `/layout`, `/side by side`, `/multi-column`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| Content shape         | Container-with-children, two levels deep and no further (`data-model.md` §3.1): a `columns` block holds 2–6 `column` blocks (its own `recordIds`), and each `column` holds an ordered list of ordinary blocks (its own `recordIds`) from the curated `ColumnChildBlockType` subset — no nested `columns`, and no reference/structural/container types (`callout`, `toggle`, `table`, `page_link`, `collection_view`, `child_pages`, `embed`, `synced_block`, `table_of_contents`) inside a column for v1                                                                                                                                                                                                                                                 |
| Conversion            | No in-place conversion to/from any other type in either direction — always inserts new (`createColumnsBlock`), never repurposes an existing block's fields, since a bare blockType flip can't produce the required column children                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| Focus/Enter/Backspace | Enter inside a column creates a new block after it in that same column (continuing a list, same convention as the top-level editor); Backspace on an empty, non-first block in a column deletes it and focuses the previous one; Backspace on a column's first block is a no-op (removing a column is the "−" control, not implicit); Escape has no columns-specific behavior beyond the existing drag-cancel/link-composer-close uses                                                                                                                                                                                                                                                                                                                   |
| Drag/multi-select     | A block drags between columns of the same `columns` block (pointer, or ArrowLeft/ArrowRight on its move handle) via `moveRecordToParent`; a whole `column`/`columns` block itself is not draggable via this mechanism in v1 — add/remove columns via the "+"/"−" controls instead                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| Configuration         | Initial column count (`columnCount`, 2–6, default 2, at creation — `create_record`'s own field, only valid when `blockType` is `columns`); afterward, columns are added/removed via `create_record`(`blockType: 'column'`)/`delete_record` on the `columns` block's own children, the same generic mechanism as everything else — no dedicated add/remove-column tool or service method. The 2–6 bound is enforced at every one of those mutation points, not just initial creation: `createColumnsBlock` validates the initial count, `createRecord` rejects a 7th column, and `deleteRecord` rejects dropping below 2 (all in `record-ops.ts`, so a direct data-layer or MCP caller can't bypass what the UI's own hidden +/− buttons only discourage) |
| Markdown              | Read-only, Pandoc-style fenced divs: `::: columns` wrapping one `::: column ... :::` per column, each column's body the newline-joined markdown of its own children (`markdown-transcoding.md`) — no parser turns this back into a columns block; an agent adds/removes/edits content the ordinary way via `create_record`/`write_record`/`delete_record` on the appropriate column id                                                                                                                                                                                                                                                                                                                                                                   |
| Agent representation  | Markdown is sufficient for content; `get_document`'s `children` field (generic, not columns-specific) additionally exposes each nested record's own structured fields (`checked`, `viewConfig`, etc. where applicable) without needing to re-parse markdown                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| Hold semantics        | A hold applies to one leaf block inside a column, exactly as it would at the top level — holding the `columns` or a `column` block itself has no special meaning (they never carry `content`/`markdown` to write) and is not implied by holding a child                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| Accessible rendering  | Each column is `role="group"` labeled "Column N of M"; the whole layout is `role="group"` labeled with the column count; the move handle's label states both same-column (Arrow Up/Down/Home/End) and cross-column (Arrow Left/Right) semantics; below the `md` breakpoint, columns stack vertically in document order — no separate accessible-mode fallback needed since stacked order already matches reading order                                                                                                                                                                                                                                                                                                                                   |

## 3.1 Fields 5–6 for every remaining shipped block type (issue #152)

Issue #152 (the per-block action menu and multi-select) is what makes fields 5 and 6 concrete for the first time — before it, "drag/multi-select/group behavior" had no multi-select to describe. The rows below cover every shipped `BlockType` not already given a full worked example in §3 above (`toggle`, `child_pages`, `columns` — `column`'s own addendum is folded into the `columns` row there). Heading levels 1–4 and the two list-item types share one row each since their behavior is identical across levels/variants.

Field 6 is uniform across almost every row on purpose: every block, regardless of type, exposes the same move handle (drag, or ArrowUp/Down/Home/End when focused — issue #40) and the same `BlockActionMenu` "…" trigger (duplicate, delete, convert, copy link, move up/down — issue #152), and participates in multi-select (Shift/Ctrl-click or Shift+Arrow on the handle, scoped to one parent container at a time) and group move/duplicate/delete the same way. A row only says something different from that baseline when the block type actually constrains it (a `columns`/`column` container, or a leaf type with no children to speak of).

Field 5's Enter/Backspace split depends only on whether a type holds free-form inline text (`STRUCTURAL_BLOCK_TYPES` in `+page.svelte`) — not on any other property of the type. `code` is text-bearing by this rule (Enter splits/Backspace joins exactly like a paragraph — the same set `rich-text-toolbar.md` §5.2/5.3 already claims); despite this, the Diagram/Mermaid row in §3 above still (currently, ahead of that block type actually shipping) describes `code` as structural for Enter/Backspace. That row predates or otherwise disagrees with `rich-text-toolbar.md`'s explicit rule and `+page.svelte`'s own `STRUCTURAL_BLOCK_TYPES` list — flagged here rather than silently perpetuated, but left uncorrected since fixing an unshipped block type's worked example is outside this issue's own scope.

| Block type(s)                                  | Focus/Enter/Backspace/Escape                                                                                                                                                                                                                                                                                 | Drag/multi-select/group                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| ---------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `paragraph`                                    | Text-bearing: Enter splits at the caret into a new `paragraph` (`rich-text-toolbar.md` §5.2); Backspace at text-start joins onto the end of the previous text-bearing block (§5.3); no Escape behavior of its own.                                                                                           | Baseline behavior (see above) — no children, no constraints beyond the enclosing container's own.                                                                                                                                                                                                                                                                                                                                                                                                     |
| `heading_1`–`heading_4`                        | Same as `paragraph` — Enter splits into a new `paragraph` (not another heading, per §5.2's "same list type when continuing a list, `paragraph` otherwise" rule); Backspace at text-start joins onto the previous block; no Escape behavior.                                                                  | Baseline behavior — no children.                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| `bulleted_list_item`, `numbered_list_item`     | Enter continues the list (new item of the same type) unless the current item is empty, in which case it exits to a `paragraph` in place (§5.1); Backspace at text-start joins onto the previous block if it holds free-form text, otherwise a no-op; no Escape behavior.                                     | Baseline behavior — no children; a `numbered_list_item`'s displayed index is derived from its position among same-typed siblings, so moving it (drag, keyboard, or as part of a selected group) recomputes every subsequent number for free.                                                                                                                                                                                                                                                          |
| `to_do`                                        | Same Enter/Backspace list behavior as the two list-item types above (`rich-text-toolbar.md` §5.1 explicitly includes `to_do`); its checkbox is a separate click affordance (`toggleTodoCheck`), not reachable via Enter/Backspace/Escape.                                                                    | Baseline behavior — no children.                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| `quote`                                        | Text-bearing, identical Enter/Backspace behavior to `paragraph`; no Escape behavior.                                                                                                                                                                                                                         | Baseline behavior — no children.                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| `callout`                                      | Text-bearing, identical Enter/Backspace behavior to `paragraph`, acting on the callout's own text; the style picker (`rich-text-toolbar.md` §7) is a separate control on the block itself, not reachable via Enter/Backspace/Escape.                                                                         | Baseline behavior — no children.                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| `divider`                                      | Structural (no stored text): Enter/Backspace act on the block as a whole (create/remove a sibling), never on inline text; no Escape behavior.                                                                                                                                                                | Baseline behavior — no children.                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| `table`                                        | Structural: Enter/Backspace act on the block as a whole, not on any cell's content; in-cell navigation/editing is internal to the table's own rendering, not part of this document-level Enter/Backspace/Escape contract; no Escape behavior.                                                                | Baseline behavior — no children in the block-tree sense (its rows/cells are the table's own internal data, not child `WorkspaceRecord`s).                                                                                                                                                                                                                                                                                                                                                             |
| `code`                                         | Text-bearing (see the flagged note above): Enter splits at the caret into a new `paragraph`; Backspace at text-start joins onto the previous block; no Escape behavior.                                                                                                                                      | Baseline behavior — no children.                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| `table_of_contents`                            | Structural, computed (no stored `content`): Enter/Backspace act on the block as a whole, matching `child_pages`'/`collection_view`'s existing treatment; no Escape behavior.                                                                                                                                 | Baseline behavior — no children; its rendered heading links are read-only navigation, not child blocks.                                                                                                                                                                                                                                                                                                                                                                                               |
| `synced_block`                                 | Structural: Enter/Backspace act on the block as a whole; no Escape behavior. Its content is a live mirror of the target record, so duplicating a `synced_block` copies the reference (`referencedRecordId`), not the target's content — the copy still points at the same source.                            | Baseline behavior — no children.                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| `page_link`                                    | Structural: Enter/Backspace act on the block as a whole; no Escape behavior.                                                                                                                                                                                                                                 | Baseline behavior — no children.                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| `embed`                                        | Structural: Enter/Backspace act on the block as a whole; no Escape behavior.                                                                                                                                                                                                                                 | Baseline behavior — no children.                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| `collection_view`                              | Structural: Enter/Backspace act on the block as a whole; no Escape behavior. Full-width toggling (issue #150) is a separate control on the block, not reachable via Enter/Backspace/Escape.                                                                                                                  | Baseline behavior — no children in the block-tree sense (the Collection's own rows aren't child `WorkspaceRecord`s of this block).                                                                                                                                                                                                                                                                                                                                                                    |
| `column` (addendum to the `columns` row in §3) | As already stated for `columns`: Enter inside a column creates a new block after it in that same column; Backspace on an empty, non-first block deletes it; Backspace on a column's first block is a no-op; a column itself has no Enter/Backspace behavior of its own (it's never directly focused/edited). | A `column`'s own children get the same move handle, action menu, and multi-select as any top-level block, scoped to that column's own sibling list — a multi-select range or group move/duplicate/delete never crosses from one column into another, or from a column into the top-level flow. Duplicating a `column` (via its parent `columns` block, since a bare `column` has no menu of its own — see `rich-text-toolbar.md` §8.1) duplicates every child inside it, recursively, with fresh ids. |

## 4. Process

A new `BlockType` proposal (PRD addition or backlog issue) references this file and is expected to sketch answers to all eleven fields before implementation begins; the issue's own "Done when" criteria should restate them concretely rather than leaving them implicit. Missing fields are a signal the block type needs more design work, not a reason to skip the row.
