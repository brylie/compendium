<script lang="ts">
	// Never actually renders — the load function in +page.server.ts always
	// redirects into a Space (#6 Phase A). Present only because SvelteKit
	// requires a +page.svelte alongside +page.server.ts for a page route.
</script>
